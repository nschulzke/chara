mod error;
mod parser;
mod scanner;
mod typechecker;
mod abstract_interpreter;


fn main() {
    // let parser = Parser::new("[a b c]").parse().unwrap();
    // println!("{:?}", super.parse("[a b c]").unwrap());
}
